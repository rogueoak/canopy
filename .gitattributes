# The union merge driver was used during the 1.0.0 build-out to auto-merge barrel and
# dependency-manifest appends across many parallel component branches. It is retired now:
# union concatenation corrupted multi-line export blocks (dropped `export {` openers) and
# duplicated a dependency key. The few remaining merges resolve these files by hand instead.
