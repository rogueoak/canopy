{
  "name": "@rogueoak/roots",
  "version": "0.0.0",
  "description": "Canopy design tokens — Style Dictionary pipeline (CSS vars, typed TS, Tailwind v4 preset).",
  "license": "MIT",
  "type": "module",
  "sideEffects": [
    "*.css"
  ],
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/tokens.d.ts",
      "import": "./dist/tokens.js"
    },
    "./tokens.css": "./dist/tokens.css",
    "./tailwind-preset.css": "./dist/tailwind-preset.css"
  },
  "scripts": {
    "build": "style-dictionary build --config style-dictionary.config.mjs && tsup",
    "lint": "eslint .",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "style-dictionary": "^4.3.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
